#!/bin/bash

# maitri:summary=Show current theme
# maitri:examples=maitri theme current

THEME_NAME_PATH="$HOME/.config/maitri/current/theme.name"

if [[ -f $THEME_NAME_PATH ]]; then
  cat $THEME_NAME_PATH | sed -E 's/(^|-)([a-z])/\1\u\2/g; s/-/ /g'
else
  echo "Unknown"
fi
